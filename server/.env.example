# server/.env 로 복사한 뒤 값 채우기 (이 파일은 예시, .env는 git에 안 올라감)
# ※ 웹서버와 DB서버는 서로 다른 위치입니다. 아래 DB_* 는 원격 DB서버 주소.
PORT=8080
DB_HOST=192.168.100.76
DB_PORT=5114
DB_USER=axdea
DB_PASSWORD=여기에_비밀번호
DB_NAME=axdea
